; Hubzilla accepts photo and file uploads through PHP, so these bound what a channel
; can store in one request. Raising them means raising the ingress body limit too.
upload_max_filesize = 100M
post_max_size = 100M
max_execution_time = 120
memory_limit = 512M
expose_php = Off
